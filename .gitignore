# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/

# Personal Claude Code settings (machine-specific, like configs/local.yaml)
.claude/settings.local.json

# Downloaded/derived market data (regenerated by running the pipeline; see configs/local.yaml
# if you point data_dir somewhere other than the default ./data)
/data/

# Machine-specific config (contains local data paths) — copy configs/local.example.yaml -> configs/local.yaml
configs/local.yaml

# Legacy pipeline output (kept out of git; regenerated by running the pipeline)
tickers.csv
order list.csv
portfolio holdings *.csv